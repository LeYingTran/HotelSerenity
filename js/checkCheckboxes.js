/**
 * Created by ltran on 10/1/15.
 */
var CheckCheckboxes = (function() {
    "use strict";
    var pub = {};

    pub.setup = function() {

    };

    /* Expose public interface. */
    return pub;
}());

/* Calls the setup function for Return when the document is ready */
$(document).ready(CheckCheckboxes.setup);